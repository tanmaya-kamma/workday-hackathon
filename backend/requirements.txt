# FastAPI + ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# MongoDB async driver (PyMongo async wrapper)
motor==3.7.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Configuration
pydantic-settings==2.7.1

# Email validation (required by Pydantic's EmailStr)
email-validator==2.3.0

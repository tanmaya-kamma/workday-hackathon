# FastAPI + ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# MongoDB (motor includes pymongo)
motor==3.7.0
pymongo

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Configuration
pydantic-settings==2.7.1
python-dotenv

# Validation
email-validator==2.3.0

# AI recommender — openai SDK used as a generic client for any
# OpenAI-compatible provider (default: Google Gemini free tier)
openai==1.59.7

# Testing
pytest
